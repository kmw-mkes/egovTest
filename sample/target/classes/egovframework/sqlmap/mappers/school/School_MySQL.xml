<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SchoolDAO">

	<select id="selectSchoolList" resultType="java.util.HashMap">
		SELECT  school_id,
				school_name,
				school_area,
				school_addr,
				school_phone
		FROM tb_school_info
	</select>
	
	<select id="selectSchoolInfo" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		SELECT  school_id,
				school_name,
				school_area,
				school_addr,
				school_phone
		FROM tb_school_info
		WHERE school_id = ${schoolId}
	</select>

</mapper>